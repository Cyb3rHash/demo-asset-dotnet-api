# .NET API container environment variables
# Request these to be set in the real .env by the orchestrator/deployment system.

# Optional: Port for Kestrel to listen on (recommended for containers)
PORT=3002
